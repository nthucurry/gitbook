input {
    file {
        start_position => "beginning"
        path => "/home/azadmin/PT1H.json"
        sincedb_path => "/dev/null"
    }
}

filter {
    json {
        source => "message"
        target => "log"
    }
}

output {
    elasticsearch {
        index => "demo-json"
        hosts => "http://t-elk:9200"
        user => "elastic"
        password => "password"
    }
    #stdout {}
}